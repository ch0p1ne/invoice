<UserControl x:Class="invoice.Views.CreateExamenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:invoice.Views" 
             xmlns:viewModel="clr-namespace:invoice.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Center" 
            HorizontalAlignment="Center"
            
            >
            <WrapPanel Orientation="Vertical" Margin="0,0,0,30">
                <Label HorizontalAlignment="Center" >Nom De l'acte</Label>
                <TextBox x:Name="txtExamenName" Text="{Binding  Examen.ExamenName ,Mode=TwoWay}" Width="270" Padding="10,5"  />
            </WrapPanel>
            <WrapPanel Orientation="Vertical" Margin="0,0,0,30">
                <Label HorizontalAlignment="Center">Reférence</Label>
                <TextBox x:Name="txtReference" Text="{Binding Examen.Reference}" Width="130" Padding="10,5" />
            </WrapPanel>
            <WrapPanel Orientation="Vertical">
                <Label HorizontalAlignment="Center">Prix à l'unité</Label>
                <TextBox x:Name="txtPrice"  Text="{Binding Examen.Price}"  MinWidth="100" MaxWidth="150" Padding="10,5" />
            </WrapPanel>

        </StackPanel>
        <WrapPanel Grid.Row="1" VerticalAlignment="Bottom" 
            HorizontalAlignment="Right"
           Margin="0,0,0,50"
            >
            <Button Style="{StaticResource buttonCrudStyle}" 
                     Content="Annuler"
                    Background="#D9D9D9"
                    Foreground="White"
                    Command="{Binding UndoAddExamenCommand}"
            >
            </Button>
            <Button Style="{StaticResource buttonCrudStyle}"
            Content="Ajouter"
            Background="#008A38"
            Foreground="White"
            Command="{Binding CreateExamenCommand}"
            >

            </Button>
        </WrapPanel>
    </Grid>
</UserControl>
