<UserControl x:Class="invoice.Views.ExamenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:invoice.Views"
             xmlns:viewmodels="clr-namespace:invoice.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontFamily="{StaticResource fontBase}">

    <UserControl.DataContext>
        <viewmodels:ExamenVM />
    </UserControl.DataContext>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Top" Grid.Row="0">
            <TextBlock Text="{Binding Title}"
                       FontSize="24" 
                       Margin="100,0,0,0"
                       FontFamily="{StaticResource fontSen}" 
                       FontWeight="SemiBold"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button BorderThickness="0"
                            Background="Transparent"
                            Padding="10,8">
                        <Button.Content>
                            <WrapPanel>
                                
                                <Image Source="/Assets/icons/add.png" Height="32"/>
                                <TextBlock Text="Ajouter"
                                           FontSize="16"
                                           Margin="10,0,0,0"
                                           FontWeight="SemiBold"
                                           Foreground="DarkOliveGreen"
                                           VerticalAlignment="Center"
                                           Cursor="Hand"
                                           />
                            </WrapPanel>
                        </Button.Content>
                    </Button>
                    
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="10*" />
                </Grid.RowDefinitions>


                <WrapPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5">
                    <TextBlock Focusable="False"
                               
                               FontWeight="SemiBold"
                               Foreground="DarkKhaki" 
                               Text="Actes d'examen"
                               FontSize="20"/>
                </WrapPanel>
                <DataGrid Grid.Row="1"
                          Background="Transparent"
                          AutoGenerateColumns="False"
                          ItemsSource="{Binding Examens}"
                          HorizontalAlignment="Stretch"
                          MinRowHeight="30"
                          BorderThickness="0"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single">

                    <!-- Style pour centrer verticalement le contenu des TextBlock dans les cellules -->
                    <DataGrid.Resources>
                        <Style x:Key="CenteredTextBlock" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="10,0,0,0" />
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="MinHeight" Value="35"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGrid.RowStyle>

                    <!-- CellStyle pour centrer verticalement le contenu de chaque cellule -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        
                        <DataGridTextColumn Width="3*" Header="Acte d'examen" Binding="{Binding ExamenName}" ElementStyle="{StaticResource CenteredTextBlock}" />
                        <DataGridTextColumn Width="1*" Header="Reference" Binding="{Binding Reference}" ElementStyle="{StaticResource CenteredTextBlock}" />
                        <DataGridTextColumn Width="1*" Header="Prix" Binding="{Binding Price}" ElementStyle="{StaticResource CenteredTextBlock}" />
                        
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
