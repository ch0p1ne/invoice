<Window x:Class="invoice.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="800" Width="1450"
        xmlns:local="clr-namespace:invoice.ViewModels"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#F6FFFF"
        WindowStyle="None">

    <Window.Resources>

        <!-- Template hiérarchique pour éléments parent + enfants -->
        <HierarchicalDataTemplate x:Key="ParentTemplate" ItemsSource="{Binding Childrens}">
            <RadioButton Content="{Binding Name}"
                         Command="{Binding DataContext.ChangeCurrentViewModelCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                         CommandParameter="{Binding ViewModel}"
                         GroupName="MenuGroup"
                         Style="{StaticResource ParentItem}"/>
            <HierarchicalDataTemplate.ItemTemplate>
                <DataTemplate>
                    <RadioButton Content="{Binding Name}"
                                 Command="{Binding DataContext.ChangeCurrentViewModelCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                 CommandParameter="{Binding ViewModel}"
                                 GroupName="MenuGroup"
                                 Style="{StaticResource ChildrenItem}"/>
                </DataTemplate>
            </HierarchicalDataTemplate.ItemTemplate>
        </HierarchicalDataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="155"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Top">
                <Image Source="/Assets/img/clima-g-logo.png" Stretch="None" Height="142"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="5,15  ,0,0" Height="500" VerticalAlignment="Top">
                <TreeView ItemsSource="{Binding MenuItems}" BorderThickness="0" 
                          VerticalAlignment="Top"
                          Background="#F6FFFF"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          ItemTemplate="{StaticResource ParentTemplate}">
                </TreeView>
            </StackPanel>

            <StackPanel Margin="0,10,0,0" Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Center">
                <Button Style="{StaticResource buttonCustomStyle}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/icons/logout.png" Height="30" Width="30"/>
                            <TextBlock Text="Déconnexion" FontSize="18" Margin="20,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                        HorizontalAlignment="Right" 
                        Margin="0,0,50,0" 
                        VerticalAlignment="Center">
                 <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center">

                        <TextBlock Text="{Binding SessionService.User.Account_name}" FontSize="15"  VerticalAlignment="Center" FontFamily="{StaticResource fontBase}" Foreground="Black"/>
                        <Image Source="/Assets/icons/call-center-agent.png" Height="24" Margin="20,0,0,0"/>
                 </WrapPanel>
                
                 <WrapPanel HorizontalAlignment="Right" Margin="0,20,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DateOnly}" FontFamily="{StaticResource fontBase}" FontSize="12" FontWeight="Light" VerticalAlignment="Center" />
                    <Image Source="/Assets/icons/calendar.png" Height="24" Margin="20,0,0,0" />
                </WrapPanel>
                
            </StackPanel>
                <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="10,0,10,30"/>
        </Grid>
    </Grid>
</Window>
