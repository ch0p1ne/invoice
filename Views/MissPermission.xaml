<UserControl x:Class="invoice.Views.MissPermission"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:invoice.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock 
                   FontSize="32"
                   TextWrapping="Wrap"
                   FontWeight="Normal"
                   FontFamily="{StaticResource fontSen}"
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center">
            <Bold>Accès non autorisé.</Bold> <LineBreak/><Span FontSize="14"> Veuillez contacter un administrateur pour verifier vos permissions.</Span>
        </TextBlock>
    </StackPanel>
</UserControl>
