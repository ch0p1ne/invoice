<UserControl x:Class="invoice.Views.UserAndRoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:invoice.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontFamily="{StaticResource fontBase}"
             Style="{StaticResource Page_Style}">
    
    <UserControl.Resources>
        <Style x:Key="SegmentButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#1E90FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header Section-->
        <Border Grid.Row="0"
                x:Name="ContainerBorder"
                Width="240" Height="35"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="White"
                CornerRadius="15">
            <Grid>
                <Border x:Name="PillIndicator"
                        Width="120"
                        Height="35"
                        CornerRadius="15"
                        Background="#1E90FF"
                        HorizontalAlignment="Left">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="PillTransform" X="0"/>
                    </Border.RenderTransform>

                </Border>

                <Grid x:Name="SegmentGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Users Button -->
                    <Button x:Name="UsersButton"
                            Content="Utilisateur"
                            Grid.Column="0"
                            Command="{Binding ShowUsersViewCommand}"
                            Tag="0"
                            Foreground="White"
                            Style="{StaticResource SegmentButton}"
                            Click="SegmentButton_Click" />

                    <!-- Roles Button -->
                    <Button x:Name="RolesButton"
                            Grid.Column="1"
                            Content="Roles"
                            Command="{Binding ShowRolesViewCommand}"
                            Tag="1"
                            Style="{StaticResource SegmentButton}"
                            Click="SegmentButton_Click" />
                </Grid>
            </Grid>

        </Border>
        
        
        <!-- Content Section-->
        <ContentControl Grid.Row="1" Content="{Binding CurrentSubViewModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
